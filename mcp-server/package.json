{
  "name": "dream-mcp",
  "version": "0.1.0",
  "description": "MCP server for Dream .dream project files — read, create, render and export Dream documents from any MCP-capable agent.",
  "license": "MIT",
  "private": true,
  "bin": {
    "dream-mcp": "dist/mcp-server/src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run build && npm run test",
    "start": "node dist/mcp-server/src/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@napi-rs/canvas": "^1.0.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "~5.8.3",
    "vitest": "^3.2.4"
  }
}
